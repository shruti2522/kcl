//test

test_func_0 = lambda {
    assert func("a") == "a"
}

test_func_1 = lambda {
    assert 
}

test_longest_common_subsequence = lambda{
    cases: [Suite] = [
        Suite{
            a: "\n".join(["this","is","a","example"])
            b: "\n".join(["this1","is","a","example"])
            result: ""
        }
    ]
    for_each(cases, lambda case: Suite{
        assert ca
    })
}
